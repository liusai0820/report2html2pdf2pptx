# 演讲稿生成 Prompt 优化总结

## 优化概览

针对 `generate_speech_script` 函数（src/v2/ai_designer.py:1022）的 prompt 进行了全面优化，重点提升工作汇报场景下演讲稿生成质量。

## 主要改进

### 1. 增加时长控制逻辑（代码行 1043-1052）

**新增功能**：根据预计演讲时长自动调整详略程度

- **短汇报（<10分钟）**：精简模式，每页 80-120 字
- **中等汇报（10-20分钟）**：适中模式，每页 100-150 字
- **长汇报（>20分钟）**：详细模式，每页 120-200 字

**优点**：
- 避免短汇报过于啰嗦或长汇报过于简略
- 让 AI 能根据实际时长调整内容密度

### 2. 增加场景类型适配（代码行 1054-1081）

**新增功能**：根据 `context.scenario` 提供差异化风格指导

支持的场景类型：
- **工作汇报/报告**：成果导向、数据为主、问题与对策
- **项目答辩/评审**：可行性论证、技术细节、预判质疑
- **述职报告**：个人贡献、成长思考、问题反思
- **一般性汇报**：保持专业客观

**优点**：
- 不同场景的语气和侧重点更加精准
- 提升演讲稿的场景适配性

### 3. 优化正文表达规范

#### 3.1 调整段落结构（代码行 1133-1137）

**旧结构**：承上启下 → 核心论点 → 数据支撑 → 小结

**新结构**：引导注意（可选）→ 核心论点 → 数据支撑 → 承上启下

**改进**：
- 增加"引导注意"环节，提升互动感
- 过渡语改为最后（更自然），且标注为可选

#### 3.2 丰富过渡语句示例（代码行 1144-1149）

**旧版本**：只有 3 个简单示例

**新版本**：按类型分类，提供 4 大类过渡语：
- 章节过渡："接下来进入第二部分..."
- 内容过渡："除此之外..."、"与此同时..."
- 强调重点："这里我想特别强调..."
- 引导注意："请大家看这张图..."

**优点**：
- 避免"接下来"满天飞
- 提供更多样化的表达选择

#### 3.3 强化数据解读指导（代码行 1151-1156）

**旧版本**：只有 2 个简单示例

**新版本**：按数据类型提供专门指导：
- 增长趋势："同比增长15%，从XXX增长到YYY，实现了稳步提升"
- 对比分析："相比去年的X%，今年提升了Y个百分点，主要得益于..."
- 占比说明："占总量的X%，位居第一/处于领先地位"
- 图表解读："从这张柱状图可以看出，三个季度呈现递增态势..."

**优点**：
- 明确要求解读数据，不只是念数字
- 提供具体的解读话术模板

### 4. 新增互动与引导技巧（代码行 1158-1171）

**全新模块**，分为 3 个部分：

- **引导注意力**："请大家看这张图表..."
- **强调重点**："这里我想特别强调..."
- **节奏把控**：[停顿]标记、复杂数据后小结

**优点**：
- 提升演讲的互动性和现场感
- 避免照念 PPT 的僵硬感

### 5. 优化禁忌清单（代码行 1237-1241）

**新增 2 条禁忌**：
- 不要只报数据不解读（强调数据必须说明意义）
- 不要过渡语单调重复（呼应丰富过渡语句的指导）

**调整**：
- "不要过于冗长" 改为由时长控制逻辑动态指导

### 6. 优化开场白和结尾

- 开场白：将单位名称改为使用变量 `{context.organization}`
- 结尾：明确要求总结采用"第一、第二、第三"的结构化方式

## 技术实现细节

### 代码结构
```python
# 1. 估算时长（保持原有逻辑）
estimated_minutes = max(5, len(pages) * 2)

# 2. 新增：根据时长确定详略级别
if estimated_minutes < 10:
    detail_level = "精简"
    detail_guide = "..."
elif estimated_minutes <= 20:
    detail_level = "适中"
    detail_guide = "..."
else:
    detail_level = "详细"
    detail_guide = "..."

# 3. 新增：根据场景类型提供差异化指导
if "汇报" in context.scenario or "报告" in context.scenario:
    scenario_guide = "工作汇报风格指导..."
elif "答辩" in context.scenario or "评审" in context.scenario:
    scenario_guide = "项目答辩风格指导..."
elif "述职" in context.scenario:
    scenario_guide = "述职报告风格指导..."
else:
    scenario_guide = "一般性汇报风格指导..."

# 4. 在 prompt 中应用这些变量
prompt = f"""
...
- **预计时长**: 约 {estimated_minutes} 分钟（详略程度：{detail_level}）

{scenario_guide}

...
- **时长控制**：{detail_guide}
...
"""
```

## 使用建议

### 1. 确保传入正确的 scenario

在调用 `generate_speech_script` 时，确保 `context.scenario` 包含关键词：
- "汇报" 或 "报告" → 触发工作汇报风格
- "答辩" 或 "评审" → 触发项目答辩风格
- "述职" → 触发述职报告风格

### 2. 预览不同时长的效果

可以测试不同页数的 PPT，观察时长控制的效果：
- 3 页 PPT → 6 分钟（精简）
- 10 页 PPT → 20 分钟（适中）
- 20 页 PPT → 40 分钟（详细）

### 3. 数据密集型内容特别适用

优化后的 prompt 对数据密集型汇报（如季度报告、年度总结）效果显著，因为：
- 强化了数据解读指导
- 提供了多种数据表达模板
- 要求必须解读数据意义

### 4. 可继续优化的方向

如果未来需要进一步优化，可以考虑：
- 根据 `organization` 调整语言风格（政府 vs 企业）
- 根据听众类型调整专业术语密度
- 添加行业特定的表达习惯

## 对比示例

### 优化前
- 时长控制：统一要求 100-200 字/页
- 场景适配：无差异化
- 数据解读：只有 2 个简单示例
- 过渡语句：示例较少
- 互动技巧：无专门指导

### 优化后
- 时长控制：3 个等级（80-120/100-150/120-200）
- 场景适配：4 种场景类型
- 数据解读：4 类数据 + 具体话术模板
- 过渡语句：4 大类 + 10+ 示例
- 互动技巧：专门模块，3 个维度

## 预期效果

1. **风格更贴合场景**：工作汇报不会像项目答辩那样过于技术化
2. **时长更合理**：短汇报简洁有力，长汇报详略得当
3. **数据解读更充分**：不只念数字，还要说明意义和趋势
4. **过渡更自然**：避免单调重复，语言更丰富
5. **互动性更强**：增加引导语，提升现场感

## 文件位置

- 修改文件：`src/v2/ai_designer.py`
- 修改函数：`generate_speech_script` (行 1022 开始)
- 主要变更区域：行 1043-1081（新增逻辑）+ 行 1103-1247（prompt 内容优化）

---

**优化完成日期**：2026-01-09
**主要优化点**：场景适配、时长控制、数据解读、互动技巧、过渡语句
**保持不变**：总体原则、基本结构、输出格式
